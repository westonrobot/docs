import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebarsGeneral: SidebarsConfig = {
  generalSidebar: [
    'operational-safety',
    'robot-maintenance'
  ],
};

export default sidebarsGeneral;