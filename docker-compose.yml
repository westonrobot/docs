services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      # Bind-mount the source so edits hot-reload.
      - .:/app
      # Keep node_modules inside the container. Without this, the bind mount
      # above shadows the modules installed during the image build and the
      # dev server starts with nothing installed. Run `docker compose build`
      # after changing dependencies.
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
